{
  "name": "@utility_belt/flags",
  "version": "1.0.0",
  "description": "tool for retrieving CLI flag data",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "ts-node src/index.ts",
    "dev": "nodemon 'index.ts' --exec 'ts-node'",
    "build": "rm -r dist && tsc",
    "test": "jest --watch",
    "cover": "jest --coverage"
  },
  "keywords": [],
  "author": "Ado",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^14.0.27",
    "jest": "^26.6.3",
    "nodemon": "^2.0.4",
    "ts-jest": "^26.5.3"
  }
}
